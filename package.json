{
  "name": "haddley-cooper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install && cd ../admin && npm install",
    "setup": "npm run install:all && cd backend && cp .env.example .env && node src/seed.js",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\" \"cd admin && npm run dev\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:admin": "cd admin && npm run dev",
    "seed": "cd backend && node src/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
